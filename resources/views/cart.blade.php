@include('header')
<style>
.card {
    border: 1px solid #ddd;
    border-radius: 8px;
    box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);
    transition: transform 0.2s ease-in-out;
}

.card:hover {
    transform: translateY(-5px);
}

.qty-input {
    text-align: center;
}

.btn-outline-secondary {
    padding: 0 10px;
    font-size: 14px;
}

.btn-success {
    background-color: #28a745;
    border-color: #28a745;
    transition: background-color 0.3s;
}

.btn-success:hover {
    background-color: #218838;
    border-color: #1e7e34;
}

h4 {
    font-weight: bold;
    font-size: 20px;
    margin-bottom: 15px;
}

h6 {
    font-size: 16px;
    font-weight: 500;
    color: #333;
}

.text-muted {
    font-size: 14px;
    color: #6c757d;
}

.card p, .card span {
    font-size: 14px;
    color: #555;
}

.text-danger {
    color: #dc3545 !important;
    text-decoration: underline;
    cursor: pointer;
    font-size: 13px;
}

.text-end {
    text-align: end;
}

.btn-block {
    width: 100%;
}

.container {
    max-width: 1140px;
}

.card-body {
    padding: 20px;
}

hr {
    border: 1px solid #E0E0E0;
    margin: 10px 0;
}

.d-flex {
    display: flex;
}

.align-items-center {
    align-items: center;
}

.justify-content-between {
    justify-content: space-between;
}

.mb-3 {
    margin-bottom: 1rem;
}

.mb-2 {
    margin-bottom: 0.5rem;
}

.ms-3 {
    margin-left: 1rem;
}

.mt-3 {
    margin-top: 1rem;
}

.mt-2 {
    margin-top: 0.5rem;
}

.small {
    font-size: 12px;
}

.card img {
    border-radius: 5px;
}
.border-gall {
            
            max-height: 450px;
            margin-bottom: 10px;
            overflow-y: scroll;
 
        }
/* New css  Start*/

body.fontLoaded {
                font-family: 'Clear Sans', 'Helvetica Neue', Helvetica, Arial, sans-serif;
            }


            .fOBHey {
    width: 100%;
    opacity: 1;
}
.summary-card-new {
    background-color: #ffffff;
    padding: 15px;
    border-radius: 5px;
    margin-bottom: 10px;
    line-height: 29px;
    border-block-color: bisque;
    box-shadow: 0 1px 1px 0 rgba(60, 64, 67, .08), 0 1px 3px 1px rgba(60, 64, 67, 0.1);
}
   @media only screen and (min-width: 768px) {
    .fAPBPH {
        font-size: 20px;
    }
}
.fAPBPH {
    font-size: 16px;
    font-weight: 700;
    color: #ffffff;
}
.fKIuWX {
    display: -webkit-box;
    display: -webkit-flex;
    display: -ms-flexbox;
    display: flex;
    overflow: auto;
}
.fKIuWX > div {
    min-width: 200px;
    width: 200px;
    display: -webkit-box;
    display: -webkit-flex;
    display: -ms-flexbox;
    display: flex;
    -webkit-align-items: center;
    -webkit-box-align: center;
    -ms-flex-align: center;
    align-items: center;
    background: #fff;
    color: #212121;
    border-radius: 4px;
    padding: 5px;
    margin: 8px;
    box-shadow: 0px 0px 8px rgb(0 0 0 / 15%);
    position: relative;
}
.fKIuWX img {
    margin: 0 7px;
    height: 75px;
}
img {
    border-style: none;
}
.fKIuWX .product-name {
    font-size: 10px;
    font-weight: 500;
    margin-bottom: 5px;
    margin-right: 32px;
    overflow: hidden;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
}
p, label {
    /* font-family: inherit; */
    margin: 0;
}

.cquXgC {
    width: 32px;
    height: 32px;
    border-radius: 8px;
    position: absolute;
    right: 5px;
    bottom: 5px;
    left: auto;
    z-index: 1;
    color: #fff;
    background: #4fcf64;
    border: #4fcf64;
    font-size: 26px;
    padding: 0px;
    font-weight: 500;
    cursor: pointer;
}
.listing {
    padding: 10px 0px 10px 10px;
    -webkit-transition: all 500ms ease-in-out;
    transition: all 500ms ease-in-out;
    background: #FE7B32;
    color: #ffffff;
    border-radius: 8px;
    box-shadow: 0px 0px 8px rgb(0 0 0 / 15%);
    margin: 0 15px 30px;
}

:after, :before {
    background-repeat: no-repeat;
    box-sizing: inherit;
}



/*Coupen Code CSS*/

.fmrEVf {
    width: 100%;
    display: -webkit-box;
    display: -webkit-flex;
    display: -ms-flexbox;
    display: flex;
    height: auto;
    -webkit-box-pack: center;
    -webkit-justify-content: center;
    -ms-flex-pack: center;
    justify-content: center;
    -webkit-flex-direction: column;
    -ms-flex-direction: column;
    flex-direction: column;
    -webkit-align-items: center;
    -webkit-box-align: center;
    -ms-flex-align: center;
    align-items: center;
    padding-bottom: 20px;
    padding-left: 15px;
    padding-right: 15px;
}

.fmrEVf > div:first-child {
    width: 100%;
    box-shadow: 0 1px 1px 0 rgba(60, 64, 67, .08), 0 1px 3px 1px rgba(60, 64, 67, 0.1);
    border-radius: 2px;
    padding-top: 15px;
    padding-bottom: 15px;
    padding-left: 15px;
    padding-right: 15px;
    background-color: #fff;
}
.iUSRwR {
    width: 100%;
}
.pdOfi {
    box-sizing: border-box;
    display: -webkit-box;
    display: -webkit-flex;
    display: -ms-flexbox;
    display: flex;
    -webkit-flex: 0 1 auto;
    -ms-flex: 0 1 auto;
    flex: 0 1 auto;
    -webkit-flex-direction: row;
    -ms-flex-direction: row;
    flex-direction: row;
    -webkit-flex-wrap: wrap;
    -ms-flex-wrap: wrap;
    flex-wrap: wrap;
    margin-right: -0.5rem;
    margin-left: -0.5rem;
}
.bERPzT {
    border-radius: 2px;
}
.increment-btn,.decrement-btn{
        padding: 5px;
    }
@media only screen and (min-device-width: 768px) {
    .bERPzT {
        margin-right: 10px;
        
    }
    .increment-btn,.decrement-btn{
        padding: 0px;
    }
    .quantity-spinner button{
        padding:0px 0px;
    }
}
.bERPzT {
    width: 100%;
    border: none;
    border-bottom: 1px solid #ccc;
    padding: 0 5px;
    height: 36px;
    font-size: 15px;
    outline: none;
}
/* For Chrome, Edge, Safari */
.qty-input::-webkit-inner-spin-button, 
.qty-input::-webkit-outer-spin-button { 
    -webkit-appearance: none; 
    margin: 0; 
}

/* For Firefox */
.qty-input[type="number"] {
    -moz-appearance: textfield;
}
.quantity-spinner {
    display: flex;
    align-items: center;
    border: 2px solid green; /* Border color */
    border-radius: 10px; /* Rounded edges */
   
    width: fit-content;
}

/* Buttons */
.quantity-spinner button {
    background: none;
    border: none;
    color: green; /* Green color for icons */
    font-size: 13px;
    cursor: pointer;
    padding: 0 5px;
}

/* Input */
.quantity-spinner .qty-input {
    width: 40px;
    text-align: center;
    border: none;
    outline: none;
    font-size: 16px;
    appearance: textfield; /* Removes default spinner */
}

/* Remove focus outline from buttons */
.quantity-spinner button:focus {
    outline: none;
}

/* For Chrome, Edge, Safari */
.quantity-spinner .qty-input::-webkit-inner-spin-button,
.quantity-spinner .qty-input::-webkit-outer-spin-button {
    -webkit-appearance: none;
    margin: 0;
}

/* For Firefox */
.quantity-spinner .qty-input[type="number"] {
    -moz-appearance: textfield;
}


</style>
<div class="container my-5">
   <div class="row">

    <div class="col-md-8">
        <div class="listing">
    <p class="CartListing__HeaderText-btcal fAPBPH">Recommended for you</p>
    <div class="CartListing__ProductListingContainer-eOzeVW fKIuWX">
        @foreach($recommendedProducts as $product)
            <div>
                <img src="{{ url('admin/assets/uploads/product/home/' . $product->pimage) }}" alt="{{ $product->name }}">
                <div>
                    <p class="product-name">{{ $product->name }}</p>
                    <b class="product-price">₹{{ number_format($product->price, 2) }}</b>
                    <button class="ProductCardComponent__AddToCartButton-fNPcVl cquXgC" onclick="showModal(event, {{ $product->id }})">+</button>
                </div>
            </div>
        @endforeach
    </div>
</div>


    </div>
    <div class="col-md-4">
    <div class="TotalsWrapper">
        <div class="ApplyCouponSection">
            <div class="CouponCodeForm">
                <div class="row">
                    <div class="col-md-8">
                        <input type="text" name="coupon" id="coupon" class="form-control" placeholder="Enter Coupon Code">
                    </div>
                    <div class="col-md-4">
                        <button type="button" id='applycode_button' class="btn btn-primary" onclick="applyPromoCode()">Apply</button>
                    </div>
                    
                    <div>
            <p id="coupon-message" style="color: green">
                
            </p>
        </div>
        <hr />
       <!-- <div>
            <p class="text-danger mb-2"><b>Inclusive of all taxes.</b></p>
        </div>
        <div class="d-flex justify-content-between">
          <p class="mb-2">Total price:</p>
          <p class="mb-2 fw-bold text-dark">₹<span id="total_price">{{number_format($payabletotal, 1) }}</span></p>
        </div>
            


                <div class="form-outline mb-3">
                    <p class="mb-0">Total Amount</p>
                    <input type="hidden" name="total_amount" id="total_amount" value="{{ $payabletotal }}">
                    <input type="text" id="total" class="form-control" value="{{ $payabletotal }}" readonly />
                </div> --> 
                <input type="hidden" name="total_amount" id="total_amount" value="{{ $subtotal }}">
                </div>
                
            </div>
        </div>
    </div>
</div>

</div>
    <div class="row">
        <div class="col-md-8 col-sm-12">
            @if(session('success'))
                <center><div id="alert-box" class="alert alert-success d-flex align-items-center justify-content-between" 
     style="border-radius: 30px; padding: 15px; font-size: 16px; color: green; width: 61%; margin: 20px auto; text-align: center;">
    <div>
        <i class="fas fa-check-circle" style="margin-right: 8px;"></i>{{ session('success') }}
    </div>
    <button type="button" class="btn-close" style="border: none; background: transparent;" onclick="this.parentElement.style.display='none';">
        <i class="fas fa-times"></i>
    </button>
</div></center>
                
               

                
            @endif

            <!-- Cart Items Section -->
            <div class="card p-3">
                <h4>Products In Your Cart</h4>
                <hr />
                @foreach($cartItems as $item)
                    <div class="d-flex justify-content-between align-items-center mb-3 mb-sm-0">
                        <div class="d-flex align-items-center" style="flex: 1;">
                            <img src="{{ url('admin/assets/uploads/product/home/' . $item->product->pimage) }}" alt="" width="70" class="rounded">
                            <div class="ms-3">
                                <h6>{{ $item->product->name }}</h6>
                                <p class="text-muted mb-0">₹{{ number_format($item->product->price, 2) }}/-</p><div class="price-tagline mt-1 mb-2">
        
        @if(($item->product->oprice - $item->product->price) > 0 ) <span class="original-price text-muted text-decoration-line-through">₹{{ $item->product->oprice }}</span>
        <span class="discount text-success"> Save ₹{{ $item->product->oprice - $item->product->price}}</span>@endif
    </div>
                            </div>
                        </div>
                       
                             <!--<div class="d-flex align-items-center mb-3 mb-sm-0" style="width: 80px;">-->
                                
                                <!--<input type="number" value="{{ $item->quantity }}" class="form-control qty-input" data-id="{{ $item->id }}" min="1" max="10" style="width: 60px;  margin-right: 10px;">-->
                               

    <div class="d-flex flex-column align-items-end mb-3 mb-sm-0" style="flex: 1;">
                            <div class="custom-spinner quantity-spinner">
        <button type="button" class="decrement-btn" data-id="{{ $item->id }}" style="border: none; background: none; font-size: 15px; color: green;">-</button>
        <input type="number" value="{{ $item->quantity }}" class="form-control qty-input" data-id="{{ $item->id }}" min="1" max="10" style="width: 40px; text-align: center; border: none; font-size: 16px;">
        <button type="button" class="increment-btn" data-id="{{ $item->id }}" style="border: none; background: none; font-size: 18px; color: green;">+</button>
    </div><br>
                            <a href="{{ route('cart.remove', ['id' => $item->product_id]) }}" class="text-danger"><i class="fas fa-trash"></i></a>
                        </div>
                    </div>
                    <hr>
                @endforeach
            </div>
        </div>

        <!-- Summary Section -->
        <div class="col-md-4 col-sm-12 mt-3 mt-md-0">
    <div class="card p-3">
        <h4>Payment Details</h4>
        
      
        <!-- MRP Value Section -->
        <div class="d-flex justify-content-between mb-2">
            <span>MRP Value</span>
            <span id="mrp-total">₹{{ number_format($total, 2) }}/-</span>
        </div>
        <div class="d-flex justify-content-between mb-2">
            <span>(-) Price Off</span>
            <span>₹{{ number_format($totalPriceOff, 2) }}/-</span>
        </div>
        <hr style='margin: 1rem 0;color: inherit; border: 0;border-top: 1px solid;opacity: .25;'>
        <div class="d-flex justify-content-between mb-2">
            <span>Subtotal</span>
            <span>₹{{ number_format($subtotal,2) }}/-</span>
        </div>
        <div class="d-flex justify-content-between mb-2">
            <span>(+) Shipping</span>
            <span id="mrp-total">₹{{ number_format($shippingCharge, 2) }}</span>
        </div>
        <div class="d-flex justify-content-between mb-2" id="coupon-applied-alert" style="visibility: hidden;">
            <span style='color:green'>(-) Coupon Applied</span>
            <span id="cop-total"></span>
        </div>
        <hr />
        
        <!-- Total Payable Amount Section -->
        <div class="d-flex justify-content-between fw-bold">
            <span>Total Payable Amount</span>
            <span id="cart-total">₹{{ number_format($payabletotal, 2) }}/-</span>
            <span id="actual_amount_store" hidden>{{ number_format($payabletotal, 2) }}</span>
        </div>
        
        <!-- Checkout Button -->
        <form action="{{ route('cart.checkout') }}" method="POST">
            @csrf
            <input type="hidden" id="discount_amt" name="discountCouponAmount" value="0">
            <button type="submit" class="btn btn-success btn-block mt-3">Proceed to Checkout</button>
        </form>
       
        <a href="{{ url('/') }}" class="btn btn-block mt-2" style='background-color: rgb(248, 90, 1); color: white;'>Back to Shop</a>
    </div>
</div>
    </div>
</div>



<!--<script>
document.addEventListener('DOMContentLoaded', function () {
    const qtyInputs = document.querySelectorAll('.qty-input');
    
    qtyInputs.forEach(input => {
        input.addEventListener('change', function () {
            const id = this.dataset.id;
            const qty = this.value;

            fetch('/cart/update', {
                method: 'POST',
                headers: {
                    'Content-Type': 'application/json',
                    'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content'),
                },
                body: JSON.stringify({
                    id: id,
                    quantity: qty,
                }),
            })
                .then(response => response.json())
                .then(data => {
                    if (data.success) {
                        window.location.reload();
                        // Update item total
                        const itemTotalElement = input.closest('.d-flex').querySelector('p.mb-0.text-end');
                        if (itemTotalElement) {
                            itemTotalElement.innerText = `₹${data.itemTotal.toFixed(2)}/-`;
                        }

                        // Update mrp total
                        const mrpTotalElement = document.getElementById('mrp-total');
                        // console.log(mrpTotalElement.innerText);
                        if (mrpTotalElement) {
                            mrpTotalElement.innerText = `₹${data.cartTotal.toFixed(2)}/-`;
                        }
                        // Update cart total
                        const cartTotalElement = document.getElementById('cart-total');
                        if (cartTotalElement) {
                            cartTotalElement.innerText = `₹${data.cartTotal.toFixed(2)}/-`;
                        }
                    } else {
                        alert('Failed to update cart. Please try again.');
                    }
                })
                .catch(error => console.error('Error:', error));
        });
    });
});

</script>-->

<script>
document.addEventListener('DOMContentLoaded', function () {
    const qtyInputs = document.querySelectorAll('.qty-input');
    const incrementButtons = document.querySelectorAll('.increment-btn');
    const decrementButtons = document.querySelectorAll('.decrement-btn');

    // Update quantity function (common for buttons and input change)
    function updateQuantity(input) {
        const id = input.dataset.id;
        const qty = input.value;

        fetch('/cart/update', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'X-CSRF-TOKEN': document.querySelector('meta[name="csrf-token"]').getAttribute('content'),
            },
            body: JSON.stringify({
                id: id,
                quantity: qty,
            }),
        })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    window.location.reload();
                    // Update item total
                    const itemTotalElement = input.closest('.d-flex').querySelector('p.mb-0.text-end');
                    if (itemTotalElement) {
                        itemTotalElement.innerText = `₹${data.itemTotal.toFixed(2)}/-`;
                    }

                    // Update cart totals
                    const mrpTotalElement = $('#mrp-total');
                    if (mrpTotalElement) {
                        mrpTotalElement.innerText = `₹${data.cartTotal.toFixed(2)}/-`;
                    }

                    const cartTotalElement = $('#cart-total');
                    if (cartTotalElement) {
                        cartTotalElement.innerText = `₹${data.cartTotal.toFixed(2)}/-`;
                    }
                } else {
                    alert('Failed to update cart. Please try again.');
                }
            })
            .catch(error => console.error('Error:', error));
    }

    // Attach event listeners to input fields
    qtyInputs.forEach(input => {
        input.addEventListener('change', function () {
            updateQuantity(this);
        });
    });

    // Attach event listeners to increment buttons
    incrementButtons.forEach(button => {
        button.addEventListener('click', function () {
            const input = this.previousElementSibling;
            const currentValue = parseInt(input.value);
            const max = parseInt(input.max);

            if (currentValue < max) {
                input.value = currentValue + 1;
                updateQuantity(input);
            }
        });
    });

    // Attach event listeners to decrement buttons
    decrementButtons.forEach(button => {
        button.addEventListener('click', function () {
            const input = this.nextElementSibling;
            const currentValue = parseInt(input.value);
            const min = parseInt(input.min);

            if (currentValue > min) {
                input.value = currentValue - 1;
                updateQuantity(input);
            }
        });
    });
});
var discountAmount = 0;
 function applyPromoCode() {
    const couponInput = $('#coupon').val(); // Get the coupon input value
    console.log(couponInput);

    $.ajax({
        url: `/coupons/get/${couponInput}`,
        method: 'GET',
        success: function (data) {
            console.log('data', data);
            if (data) {
                console.log('Valid coupon:', data);

                // Calculate the discount amount
                 discountAmount = data.percentage 
                    ? (data.percentage / 100) * parseFloat($('#total_amount').val()) 
                    : data.price;

                const newTotal = parseFloat($('#total_amount').val()) - discountAmount;

                // Update UI
                $('#cart-total').html('₹' + (newTotal + <?php echo $shippingCharge; ?>).toFixed(2) + '/-');
                $('#cop-total').html('₹' + discountAmount.toFixed(2) + '/-');
                $('#coupon-message').html(`Coupon applied successfully! <a href="javascript:;" style="color: red" onclick="removeCoupone(${discountAmount}, ${newTotal})">Remove</a>`).css('color', 'green');

                // Hide apply button and show applied alert
                $('#applycode_button').hide();
                $('#coupon-applied-alert').css('visibility', 'visible');
                $('#discount_amt').val(discountAmount.toFixed(2));
            } else {
                $('#coupon-message').html('Invalid coupon code.').css('color', 'red');
            }
        },
        error: function (xhr, status, error) {
            console.error('Error:', error);
            // alert('Failed to apply coupon.');
            $('#coupon-message').html('Invalid coupon code.').css('color', 'red');
        }
    });
}

  function removeCoupone(discountAmount,new_total) {
     $('#coupon-message').html('');
     $('#applycode_button').css('display', 'block');
     $('#coupon-applied-alert').css('visibility', 'hidden');
     const newTotal = parseFloat($('#total_amount').val()) + discountAmount;
     $('#cart-total').html('₹' + ($('#actual_amount_store').html()) +'/-');
     $('#discount_amt').val('0');
  }

  function proceedToCheckout() {
    location.href='/checkout/'
  }

    // Hide the alert div after 3 seconds
    setTimeout(function() {
        const alertBox = document.getElementById('alert-box');
        if (alertBox) {
            alertBox.style.transition = 'opacity 0.5s'; // Smooth fade-out effect
            alertBox.style.opacity = '0';
            setTimeout(() => alertBox.style.display = 'none', 500); // Hide it completely after fade-out
        }
    }, 3000);
</script>
@include('footer')
